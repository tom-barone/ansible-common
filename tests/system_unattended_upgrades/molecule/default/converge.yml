---
- name: "Converge"
  hosts: "all"
  tasks:
    - name: "Apply role under test"
      ansible.builtin.include_role:
        name: "system_unattended_upgrades"
      vars:
        system_unattended_upgrades_origins:
          - "origin=Proxmox"
