---
- name: "Converge"
  hosts: "all"
  tasks:
    - name: "Apply role under test"
      ansible.builtin.include_role:
        name: "user_add_to_groups"
      vars:
        user_add_to_groups_username: "testuser"
        user_add_to_groups_groupnames: ["group1", "group2"]
