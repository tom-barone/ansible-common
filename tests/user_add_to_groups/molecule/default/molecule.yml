---
driver:
  name: docker
platforms:
  - name: "debian12-${TEST_NAME}"
    image: "geerlingguy/docker-debian12-ansible:latest"
    pre_build_image: true
provisioner:
  name: ansible

scenario:
  test_sequence:
    # - dependency
    # - cleanup
    - destroy
    - syntax
    - create
    # - prepare
    - converge
    - idempotence
    # - side_effect
    - verify
    # - cleanup
    - destroy
