---
- name: Converge
  hosts: all
  tasks:
    - name: Apply role under test
      ansible.builtin.include_role:
        name: docker_install
      vars:
        docker_install_version: "5:29.2.1-1~debian.13~trixie"
