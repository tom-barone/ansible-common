---
- name: "Create QEMU VM"
  hosts: "localhost"
  connection: "local"
  gather_facts: false
  tasks:
    - name: "Create VM"
      ansible.builtin.include_role:
        name: "qemu_vm_create"
      vars:
        qemu_vm_create_name: "segoffload-vm"
        qemu_vm_create_ssh_port: 2233
