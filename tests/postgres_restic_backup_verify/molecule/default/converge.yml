---
- name: "Converge"
  hosts: "all"
  tasks:
    - name: "Apply role under test"
      ansible.builtin.include_role:
        name: "postgres_restic_backup_verify"
      vars:
        postgres_restic_backup_verify_queries:
          - "SELECT count(*) FROM users"
