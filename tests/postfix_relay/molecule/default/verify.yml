---
- name: "Verify"
  hosts: "all"
  tasks:
    - name: "Check postfix package is installed"
      ansible.builtin.command: "dpkg -s postfix"
      changed_when: false

    - name: "Check libsasl2-modules package is installed"
      ansible.builtin.command: "dpkg -s libsasl2-modules"
      changed_when: false

    - name: "Read main.cf file"
      ansible.builtin.slurp:
        src: "/etc/postfix/main.cf"
      register: "main_cf"

    - name: "Assert main.cf has correct configuration"
      vars:
        content: "{{ main_cf.content | b64decode }}"
      ansible.builtin.assert:
        that:
          - "'relayhost = [smtp.gmail.com]:587' in content"
          - "'inet_protocols = ipv4' in content"
          - "'smtp_sasl_auth_enable = yes' in content"
          - "'smtp_generic_maps = hash:/etc/postfix/generic' in content"
        msg: "main.cf missing expected configuration"

    - name: "Check generic map file exists with correct permissions"
      ansible.builtin.stat:
        path: "/etc/postfix/generic"
      register: "generic_map"

    - name: "Assert generic map permissions"
      ansible.builtin.assert:
        that:
          - "generic_map.stat.exists"
          - "generic_map.stat.mode == '0644'"
        msg: "generic map file missing or has incorrect permissions"

    - name: "Check generic map hash db exists"
      ansible.builtin.stat:
        path: "/etc/postfix/generic.db"
      register: "generic_map_db"

    - name: "Assert generic map hash db exists"
      ansible.builtin.assert:
        that:
          - "generic_map_db.stat.exists"
        msg: "generic.db missing - postmap did not run"

    - name: "Read generic map file"
      ansible.builtin.slurp:
        src: "/etc/postfix/generic"
      register: "generic_map_content"

    - name: "Assert generic map has expected entry"
      vars:
        content: "{{ generic_map_content.content | b64decode }}"
      ansible.builtin.assert:
        that:
          - "'@example.internal noreply@example.com' in content"
        msg: "generic map missing expected entry"

    - name: "Check sasl_passwd file exists with correct permissions"
      ansible.builtin.stat:
        path: "/etc/postfix/sasl/sasl_passwd"
      register: "sasl_passwd"

    - name: "Assert sasl_passwd permissions"
      ansible.builtin.assert:
        that:
          - "sasl_passwd.stat.exists"
          - "sasl_passwd.stat.mode == '0600'"
        msg: "sasl_passwd file missing or has incorrect permissions"

    - name: "Read aliases file"
      ansible.builtin.slurp:
        src: "/etc/aliases"
      register: "aliases"

    - name: "Assert aliases has correct root alias"
      vars:
        content: "{{ aliases.content | b64decode }}"
      ansible.builtin.assert:
        that:
          - "'root: admin@example.com' in content"
        msg: "aliases missing expected root alias"
