---
# Ansible role to install helpful system tools

- name: "Install system tools"
  ansible.builtin.apt:
    name:
      - "btm"
    state: "present"
    update_cache: true
    cache_valid_time: 3600
