# Managed by Ansible role: restic_backup
[Unit]
Description=Restic backup timer ({{ restic_backup_job_name }})

[Timer]
OnCalendar={{ restic_backup_timer_schedule }}
Persistent=true

[Install]
WantedBy=timers.target
