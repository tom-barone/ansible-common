{% for key, value in restic_backup_env.items() %}
export {{ key }}="{{ value }}"
{% endfor %}
export RESTIC_REPOSITORY="{{ restic_backup_repository }}"
export RESTIC_PASSWORD="{{ restic_backup_restic_password }}"
