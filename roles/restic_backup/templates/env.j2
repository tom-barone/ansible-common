{% for key, value in restic_backup_env.items() %}
export {{ key }}={{ value | quote }}
{% endfor %}
export RESTIC_CACHE_DIR={{ restic_backup_dir }}/cache
export RESTIC_REPOSITORY={{ restic_backup_repository | quote }}
export RESTIC_PASSWORD={{ restic_backup_restic_password | quote }}
