export AWS_ACCESS_KEY_ID="{{ postgres_restic_backup_s3_access_key }}"
export AWS_SECRET_ACCESS_KEY="{{ postgres_restic_backup_s3_secret_key }}"
export RESTIC_REPOSITORY="s3:{{ postgres_restic_backup_s3_endpoint }}/{{ postgres_restic_backup_s3_bucket }}"
export RESTIC_PASSWORD="{{ postgres_restic_backup_restic_password }}"
